import React from 'react';
import { List } from 'semantic-ui-react';

export const MovieModal = (props) => {
    // const { results } = props
    return (
        <React.Fragment>
            <List divided>

            </List>
        </React.Fragment>
    )
}